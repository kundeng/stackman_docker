version: "3.3"

services:
  ###############################################
  ####              EXAMPLE                 #####
  ###############################################
  EXAMPLE:
    image: EXAMPLE/EXAMPLE
    container_name: EXAMPLE
    restart: unless-stopped
    networks:
      - proxy

networks:
  proxy:
    external: true
    name: ${PROXY_NETWORK:-proxy}

